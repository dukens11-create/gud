import 'package:flutter/material.dart';

class CreateLoadScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Load'),
      ),
      body: Center(
        child: Text('Load Creation Screen'),
      ),
    );
  }
}