name: gud_app
description: GUD Express Trucking Management App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.4
  
  # Utilities
  image_picker: ^1.1.2
  intl: ^0.19.0
  
  # Web-specific (optional, for better web support)
  url_strategy: ^0.2.0  # For clean URLs (no # in URL)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
