name: gud_app
description: GUD Express Trucking Management App Demo
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase - Updated for Flutter 3.24.0 web compatibility
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.0
  firebase_storage: ^12.3.0
  
  # Utilities
  image_picker: ^1.1.2
  intl: ^0.19.0
  url_strategy: ^0.2.0  # For clean PWA URLs
  geolocator: ^10.1.0  # GPS location tracking

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
