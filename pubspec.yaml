name: my_app
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # flutter_background_geolocation: ^4.16.2  # DISABLED - Requires paid license
  another_dependency: ^1.0.0

flutter:
  uses-material-design: true