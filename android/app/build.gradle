android {
    buildTypes {
        release {
            minifyEnabled false
            shrinkResources false
        }
    }

    packagingOptions {
        pickFirst '**/*.so'
        pickFirst '**/*.txt'
    }
}